// src/App.test.tsx

// import { screen } from '@testing-library/react';
// import { renderWithRouter } from "../utils/__mocks__/renderWithRouter";
// import App from '../../App';

// it('Navega para a página PageForm2', async () => {
//   const { user } = renderWithRouter(<App />);

//   expect(screen.getByText(/Queremos muito te conhecer/i)).toBeInTheDocument();

//   const nextLink = screen.getByRole('navigate', { name: /Próximo/i });
//   await user.click(nextLink);
//   expect(screen.getByText(/Selecione o campo que corresponde ou chegue mais perto do seu tempo na área de tecnologia./i)).toBeInTheDocument();
// });